<Window x:Class="Benner.Views.PessoaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Benner.ViewModels"
        mc:Ignorable="d"
        Title="Cadastro de Pessoas" Height="500" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:PessoaViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Nome:" VerticalAlignment="Center"/>
            <TextBox Width="150" Margin="5,0" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="CPF:" VerticalAlignment="Center"/>
            <TextBox Width="120" Margin="5,0" Text="{Binding FiltroCPF, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Filtrar" Width="80" Margin="10,0,0,0" Command="{Binding FiltrarCommand}"/>
            <Button Content="Limpar Filtro" Width="100" Margin="5,0,0,0" Command="{Binding LimparFiltroCommand}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Nome:" VerticalAlignment="Center"/>
            <TextBox Width="150" Margin="5,0" Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="CPF:" VerticalAlignment="Center"/>
            <TextBox Width="120" Margin="5,0" Text="{Binding CPF, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Endereço:" VerticalAlignment="Center"/>
            <TextBox Width="200" Margin="5,0" Text="{Binding Endereco, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="{Binding TextoBotaoSalvar}" Width="120" Margin="10,0,0,0" Command="{Binding SalvarCommand}"/>
            <Button Content="Limpar" Width="120" Margin="10,0,0,0" Command="{Binding LimparCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  SelectionMode="Single" 
                  SelectionUnit="FullRow"
                  ItemsSource="{Binding Pessoas}"
                  SelectedItem="{Binding PessoaSelecionada}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="120"/>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*"/>
                <DataGridTemplateColumn Header="Ações" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar" Width="60" Margin="2" 
                                        Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Excluir" Width="60" Margin="2" 
                                        Command="{Binding DataContext.ExcluirCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Incluir Pedido" Width="100" Margin="2"
                                        Command="{Binding DataContext.IncluirPedidoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
